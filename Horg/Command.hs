module Command where

-- there is a language to be parsed!!!
-- there are two arguments: --o={dot,json,org}
--                          --f={deep,surface,preserve}
--                          -- filenames
